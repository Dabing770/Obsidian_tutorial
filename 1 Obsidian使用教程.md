# Obsidian 使用教程

> 一份从零开始的完整入门指南，带你从「建库」到「用好双链」，再到「插件进阶」。你甚至可以把这份文档当作练习素材，边看边改。

---

## 目录

1. [什么是 Obsidian](#什么是-obsidian)
2. [安装与首次启动](#安装与首次启动)
3. [核心概念：仓库（Vault）](#核心概念仓库vault)
4. [创建与管理笔记](#创建与管理笔记)
5. [Markdown 语法速成](#markdown-语法速成)
6. [双向链接 [[笔记]]](#双向链接-笔记)
7. [标签 #标签](#标签-标签)
8. [关系图谱（Graph View）](#关系图谱graph-view)
9. [搜索与快速切换](#搜索与快速切换)
10. [常用核心插件](#常用核心插件)
11. [常用快捷键](#常用快捷键)
12. [社区插件推荐](#社区插件推荐)
13. [同步与发布](#同步与发布)
14. [实用技巧与好习惯](#实用技巧与好习惯)

---

## 什么是 Obsidian

**Obsidian** 是一款**本地优先**的笔记软件，核心特点：

- **文件就是纯文本 Markdown**：你的笔记本质上是 `.md` 文件，保存在你自己电脑的文件夹里，不绑定任何云端服务，随时可以迁移、备份、用任何编辑器打开。
- **双向链接**：笔记之间可以互相引用（`[[另一篇笔记]]`），从而编织成一张「知识网络」，而不是一堆孤立的文档。
- **关系图谱**：把笔记之间的链接可视化，直观看到你的知识结构。
- **完全可扩展**：强大的插件生态，可以把它改造成日记本、任务管理器、知识库、写作工具等。

> **一句话总结**：Obsidian = 你本地文件夹里的 Markdown 文件 + 强大的链接和视图。

---

## 安装与首次启动

1. 前往官网 <https://obsidian.md> 下载对应系统的安装包（Windows / macOS / Linux）。
2. 安装并打开 Obsidian。
3. 首次启动会让你选择语言，然后**创建一个仓库（Vault）**，或打开已有文件夹作为仓库。
4. 创建仓库时，选择（或新建）一个**本地文件夹**作为笔记的存放位置。
5. 进入后你会看到默认界面：左侧文件列表、中间编辑区、右侧可开关的面板。

---

## 核心概念：仓库（Vault）

- **Vault（仓库 / 库）**：就是一个存放你所有 Markdown 文件的文件夹。整个 Obsidian 都是围绕这个文件夹工作的。
- 仓库里可以自由建子文件夹组织笔记，例如：

  ```
  MyVault/
  ├── 日记/
  ├── 学习笔记/
  ├── 工作/
  └── 收件箱/
  ```

- 每个仓库有自己独立的设置和插件，位于仓库根目录下的 `.obsidian` 文件夹（隐藏文件夹）。
- **建议**：一个仓库对应一个领域/用途，避免把毫无关联的内容塞进同一个库。

---

## 创建与管理笔记

- **新建笔记**：点击左侧文件列表顶部的「新建笔记」图标，或按 `Ctrl/Cmd + N`。
- **重命名**：在文件列表里右键笔记 → 重命名，或选中标题后按 `F2`。
- **移动**：直接把文件拖到目标文件夹；已经指向这篇笔记的链接会自动更新（Obsidian 会维护链接关系）。
- **删除**：右键 → 删除。被删除的文件会进入系统回收站，可以从「设置 → 文件恢复」或系统回收站找回。
- **笔记标题**：通常用第一行的 `# 标题` 作为标题，但文件名才是笔记的真正身份（链接靠文件名）。

> 小提示：文件名的命名建议统一风格（如 `2026-01-01 会议记录`），方便日后搜索和排序。

---

## Markdown 语法速成

Obsidian 使用 Markdown 语法，编辑时可即时预览（Live Preview）。以下是常用语法：

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 强调

```markdown
**加粗**
*斜体*
~~删除线~~
==高亮==
```

### 列表

```markdown
- 无序列表项
- 无序列表项
  1. 嵌套有序项
  2. 嵌套有序项

1. 有序列表
2. 有序列表
```

### 待办清单（Checkbox）

```markdown
- [ ] 未完成的任务
- [x] 已完成的任务
```

### 引用

```markdown
> 这是一段引用文字。
```

### 代码

````markdown
行内代码：`print("hello")`

代码块：
```python
def hello():
    print("你好，Obsidian")
```
````

### 链接

```markdown
[外部链接](https://obsidian.md)
[[内部笔记链接]]
![[嵌入某篇笔记]]
```

### 表格

```markdown
| 名称 | 快捷键 | 作用 |
| ---- | ------ | ---- |
| 命令面板 | Ctrl/Cmd + P | 打开所有命令 |
| 快速切换 | Ctrl/Cmd + O | 快速打开笔记 |
```

### 分隔线

```markdown
---
```

> 完整语法参考：Obsidian 官方帮助文档（可在软件内按 `Ctrl/Cmd + P` 输入「帮助」打开）。

---

## 双向链接 [[笔记]]

这是 Obsidian 最核心、区别于普通笔记软件的功能。

### 创建链接

在笔记中输入 `[[`，会弹出笔记搜索框，选择目标笔记即可：

```markdown
今天读到了关于 [[学习方法]] 的内容，和之前的 [[费曼技巧]] 有联系。
```

- 链接的目标笔记**不必先存在**，点击链接可以自动创建它。
- 链接也可以带别名：`[[笔记名|显示的文字]]`。
- 链接到标题：`[[笔记名#某个标题]]`。
- 链接到段落：`[[笔记名#^段落ID]]`（在段落末尾输入 `^` 会自动生成 ID）。

### 反向链接（Backlinks）

打开某篇笔记，在右侧面板能看到「反向链接」：**所有提到这篇笔记的其他笔记**。这是双向链接的「双向」所在——你不仅能看出去，还能看到谁链接了过来。

### 嵌入

用 `![[笔记名]]` 可以把整篇笔记的内容**嵌入**当前笔记直接显示；`![[笔记名#标题]]` 只嵌入某一段。

---

## 标签 #标签

标签用于给笔记打上「分类 / 状态 / 主题」标记：

```markdown
#学习 #待办 #2026
```

- 在正文任意位置输入 `#` 后跟文字即可创建标签。
- 标签支持层级：`#学习/编程`、`#学习/英语`。
- 点击任意标签，或打开右侧「标签」面板，可以查看所有带该标签的笔记。
- 标签更适合表达「类型 / 状态」，而链接更适合表达「笔记之间的具体关系」。两者配合使用效果最好。

---

## 关系图谱（Graph View）

点击左侧的「关系图谱」图标（或 `Ctrl/Cmd + G`），可以看到所有笔记以「点」呈现、链接以「线」相连的网络图。

- 点越大、越靠中心，通常表示被链接得越多（越重要）。
- 右上角可以按标签、文件路径、关键词等**过滤**显示范围。
- 用它可以发现自己知识库中的「孤岛笔记」（没有链接的笔记）和「枢纽笔记」（被大量链接的笔记）。

---

## 搜索与快速切换

- **快速切换**：`Ctrl/Cmd + O`，输入文件名关键词，秒开任意笔记。
- **全文搜索**：`Ctrl/Cmd + Shift + F`，在所有笔记内容里搜索关键词。
- **当前笔记搜索**：`Ctrl/Cmd + F`。
- **高级搜索**：搜索框支持多种运算符，例如：
  - `file:日记` —— 只搜「日记」文件夹
  - `tag:#学习` —— 只搜带某标签的笔记
  - `line:(关键词)` —— 只匹配同一行

---

## 常用核心插件

Obsidian 自带的核心插件可在「设置 → 核心插件」里开关。常用几个：

| 插件 | 作用 |
| ---- | ---- |
| **日记（Daily Notes）** | 每天一键生成一篇日记笔记，适合记录日常、会议、日志 |
| **模板（Templates）** | 预设笔记模板，新建时一键套用 |
| **大纲（Outline）** | 右侧显示当前笔记的标题结构，快速跳转 |
| **画布（Canvas）** | 无限画布，自由摆放卡片、连线，做头脑风暴 |
| **关系图谱** | 前面介绍的笔记网络图 |
| **书签（Bookmarks）** | 收藏常用笔记，方便快速访问 |
| **文件恢复** | 自动快照，防止误删/崩溃丢失 |
| **字数统计** | 右下角实时显示字数 |

> **入门必开**：日记、模板、大纲、书签。其余按需开启。

---

## 常用快捷键

| 快捷键 | 功能 |
| ------ | ---- |
| `Ctrl/Cmd + P` | 命令面板（几乎所有功能都能从这里调用） |
| `Ctrl/Cmd + O` | 快速切换笔记 |
| `Ctrl/Cmd + N` | 新建笔记 |
| `Ctrl/Cmd + Shift + F` | 全局搜索 |
| `Ctrl/Cmd + F` | 当前笔记内搜索 |
| `Ctrl/Cmd + G` | 打开关系图谱 |
| `Ctrl/Cmd + B` / `Ctrl/Cmd + I` | 加粗 / 斜体 |
| `Ctrl/Cmd + K` | 插入链接 |
| `Ctrl/Cmd + ,` | 打开设置 |
| `Ctrl/Cmd + Shift + V` | 粘贴为纯文本 |

> 在「设置 → 快捷键」里可以自定义任意快捷键。

---

## 社区插件推荐

社区插件是第三方开发的功能扩展，在「设置 → 社区插件」里开启（首次使用需关闭「安全模式」）。热门推荐：

| 插件 | 作用 |
| ---- | ---- |
| **Dataview** | 把笔记当数据库查询，自动生成列表/表格（如「所有未完成任务」） |
| **Templater** | 比官方模板更强的模板引擎，支持脚本、变量 |
| **Calendar** | 日历视图，配合日记插件快速跳转到某天 |
| **Excalidraw** | 手绘风格的绘图工具，适合画流程图、示意图 |
| **Tasks** | 全局任务管理，聚合所有笔记里的 `- [ ]` 待办 |
| **Excalidraw / Kanban** | 看板视图，管理项目进度 |

> 初学者建议先熟悉核心功能，再按需安装插件，避免「插件太多反而不知道怎么写笔记」。

---

## 同步与发布

Obsidian 本身免费且数据都在本地，云端功能为官方付费服务（可选）：

- **Obsidian Sync**：官方端到端加密同步服务，在手机、平板、多台电脑间同步笔记。
- **Obsidian Publish**：把笔记发布成网页，公开分享。

**免费替代方案**：由于笔记就是普通文件，你也可以用网盘（iCloud、坚果云、OneDrive 等）或 Git 来同步整个仓库文件夹。

---

## 实用技巧与好习惯

1. **先写，再整理**：不要一开始就纠结完美分类，先记下来，之后用链接和标签慢慢编织结构。
2. **多用双链代替复杂文件夹**：一篇笔记可以同时属于多个主题，靠 `[[链接]]` 关联，而不是复制多份。
3. **统一命名规范**：日期用 `YYYY-MM-DD`，标题清晰可搜索。
4. **善用日记**：把琐碎想法、临时记录都丢进每日日记，重要内容再单独成篇。
5. **定期整理孤岛笔记**：在关系图谱里找出没有链接的笔记，思考它们应该和什么联系起来。
6. **勤用命令面板**：`Ctrl/Cmd + P` 是万能入口，忘了快捷键就找它。

---

## 开始动手：5 分钟练习

1. 新建一篇笔记，命名为「我的第一张卡片」。
2. 在里面写 3 条带 `[[链接]]` 的句子（链接到不存在的笔记也行）。
3. 给笔记加上标签，比如 `#练习`。
4. 打开关系图谱，看看刚创建的链接长什么样。
5. 回到本篇，用 `Ctrl/Cmd + P` 搜索并运行「日记」命令，创建今天的第一篇日记。

> 恭喜，你已经掌握 Obsidian 的基本用法。接下来就是坚持记录，让笔记在链接中慢慢生长。

---

*本教程为入门指南，更深入的功能（如 Dataview 查询、Zettelkasten 卡片笔记法）可查阅 Obsidian 官方帮助与社区教程。*
